#include "op.h"
/**
 * mul - A function that adds two numbers
 * @a: first number
 * @b: second number
 * Return: - returns the multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
