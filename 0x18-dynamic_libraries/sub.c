#include "op.h"
/**
 * sub - A function that subtracts two numbers
 * @a: first number
 * @b: second number
 * Return: returns the difference of a and b
 */

int sub(int a, int b)
{
	return (a - b);
}
