#include <stdio.h>
/**
 * main()- This function uses function 'puts' to write a text to screen
 *
 * Return: 0 returns 0 if code compiles without error
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
